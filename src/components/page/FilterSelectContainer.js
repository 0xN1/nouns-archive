export default function FilterSelectContainer({ children }) {
    return (
        <div className="flex w-full flex-auto items-center justify-center gap-4 px-8 sm:w-2/3 sm:flex-row">
            {children}
        </div>
    )
}
