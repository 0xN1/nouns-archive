export default function FilterSelectContainer({ children }) {
    return (
        <div className="flex w-1/2 flex-row items-center justify-center gap-4">
            {children}
        </div>
    )
}
